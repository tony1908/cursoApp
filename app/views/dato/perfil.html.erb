<div>
	<h1>Mis datos</h1>
	<p><%= current_user.email%></p>
	<p><%= current_user.nickname%></p>
	<%= image_tag(current_user.avatar,  size: "100x100", alt: current_user.nickname) %>
	<%= link_to('Editar perfil', edit_user_registration_path)  %> 
</div>